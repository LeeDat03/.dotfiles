-- Set a global leader key for mappings
-- Must be set before plugins are loaded
vim.g.mapleader = " "
vim.g.maplocalleader = " "

-- A flag to indicate that we have a nerd font installed
-- Useful for plugins that can display special icons
vim.g.have_nerd_font = true
